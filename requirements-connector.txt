# Connector (devbox agent) dependencies.
# Install on the machine that runs the local PTY connector:
#   pip install -r requirements.txt -r requirements-connector.txt
# pywinpty is Windows-only; on Linux/macOS the connector uses the stdlib pty.
httpx>=0.27
websockets>=12.0
PyYAML>=6.0
pywinpty>=2.0 ; sys_platform == "win32"
